###################################################


ERAS

*** Era 1
  No murderer.
  Memory rent fixed. Bid fixed. Pay fixed.
  doMob:
    gets paid
    self-posts with prop of cash like prop of sizes of Mob and Msg plus cost of doMob
    yields with prob pY dep on cash.
    mutably reproduces with genesis birth cash.
    child-posts with usual birth cash

*** Later:
  limited market, so pay decreases
  memory quota

FINALE

Basics:
1. Make one message, run one worker thread. See it processed. Check CPU charged. Wait memory bankruptcy. Exit.
2. Two messages:
     One more work. See it die first.
     Different bids. Higher more likely.
     Different message balance. Message dies of memory rent before being processed.

Lang:
1. Send a message to self, others
2. Reserve a block
3. Reproduce


###################################################

Make sure cash of drawn ticket is correct after billing memory rent on the message.

Cash back on robbing.
Thread protection for globals

Finish raffle and hotel tests. Raffle tests:
  empty, just 1, drain
  equal weights: sorted/unsorted species
  diff weights
  Multiple setup/measure functions as in some other test file

Need accounting. System animals as accounts. Rob proceeds.
Free/alloc only write ghost and free *. Preserve most stuff
Get rid of name for now (later make flame (flags+name) at mob level (2 flag bits for interpreter inc system animal in C) )

? Obligatory genesis program: no virgin pile in main program. Simplifies hotel mess.

AFTER RAFFLE: MESSAGING:
  Function of cpu bid to weight.
  Function to make a message. Test they die of memory rent, which should be proportional to size.
  Workers pull them within semaphore or something.
  Sweat. Charge cpu fee. Read sweat time from message. Keep change.

Tidying:
  Another look at include structure and tests
  More statics in templates, fewer XX names

=====================

Test that meapInsert returns whether or not lowest changed
Non-pedantic build
Index type overused
? Pile doesn't need recLen - use sizeof(XX)

Pile: pointer to index? But why?
