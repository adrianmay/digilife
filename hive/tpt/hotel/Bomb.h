// #pragma once

#include "XX_pile/1.h"
typedef struct {XXIx who; Tocks tocks;} XXBomb;
// Since the first bit of who must be 0 (cos ipile uses it for freeness)
// we're limited to 2,147,483,648 slots

